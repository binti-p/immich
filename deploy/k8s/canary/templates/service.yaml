apiVersion: v1
kind: Service
metadata:
  name: aesthetic-service-canary
  namespace: aesthetic-hub-canary
  labels:
    app: aesthetic-service
    track: canary
spec:
  type: ClusterIP
  selector:
    app: aesthetic-service
    track: canary
  ports:
    - name: http
      port: {{ .Values.aestheticService.port }}
      targetPort: {{ .Values.aestheticService.port }}
{{- if .Values.triton.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: triton-canary
  namespace: aesthetic-hub-canary
  labels:
    app: triton
    track: canary
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "{{ .Values.triton.metricsPort }}"
spec:
  type: ClusterIP
  selector:
    app: triton
    track: canary
  ports:
    - name: http
      port: {{ .Values.triton.port }}
      targetPort: {{ .Values.triton.port }}
    - name: grpc
      port: 8001
      targetPort: 8001
    - name: metrics
      port: {{ .Values.triton.metricsPort }}
      targetPort: {{ .Values.triton.metricsPort }}
{{- end }}
