# NOTE: This file is a template. The actual secret must be created with:
#   kubectl create secret generic grafana-secret \
#     --namespace monitoring \
#     --from-literal=admin-password=<your-password>
# Do NOT commit real credentials to Git.
#
# For reproducibility in CI, use the Ansible playbook which generates
# and injects the password at deploy time.
#
# This placeholder is left here to document the expected secret shape.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: monitoring
  annotations:
    note: "Created by deploy_monitoring.yml Ansible playbook — do not commit real values"
type: Opaque
stringData:
  admin-password: REPLACE_VIA_ANSIBLE
